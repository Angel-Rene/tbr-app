<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="/css/style.css">
    <title><%= book.title %></title>
</head>
<body>
    <a href="/library/">Back to Library</a>
    <h1><%= book.title %></h1>
    <h3><%= book.author %></h3>
    <% if (book.haveReady) { %>
        <p>You have read this book before!</p>
      <% } else { %>
        <p>You have not read this book yet!</p>
    <% } %>

    <form action="/library/<%=book._id%>?_method=DELETE" method="POST">
        <button type="submit">Delete <%= book.title %></button>
    </form>
    <a href="/library/<%= book._id %>/edit">Edit <%= book.title %></a>
    <a href="/library">Back to Library</a>
    
</body>
</html