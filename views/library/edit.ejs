<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editing <%= book.title %></title>
</head>
<body>
    <h1>Edit <%= book.title %></h1>
    <form action="/library/<%=book._id%>?_method=PUT" method="POST">
        <label for="title">Title:</label>
        <input type="text" name="title" id="title" value="<%= book.title %>"/>

        <label for="author">Author:</label>
        <input type="text" name="author" id="author" value="<%= book.author %>"/>

        <label for="genre">Genre:</label>
        <input type="text" name="genre" id="genre" value="<%= book.genre %>"/>

        <label for="rating">Rate the Book:</label>
        <input type="range" name="rating" id="rating" min="1" max="5" step="1"/>
        <output class="rating-output" for="rating">Stars: </output>

        <label for="haveRead">Have you read this already?</label>
        <input type="checkbox" name="haveRead" id="haveRead"  <% if (book.haveRead) { %>checked<% } %>/>

        <button type="submit">Add Book</button>
        <a href="/library/">Back to Library</a>
    </form>
</body>
</html>